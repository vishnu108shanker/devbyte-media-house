// Control Center — Overview (/control)
// Phase 4 will build the real last-run summary and per-platform counts.
export default function ControlOverviewPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Overview</h1>
      <p className="mt-4 text-gray-400">Phase 4 — coming soon.</p>
    </main>
  );
}
