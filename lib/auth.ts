// Session / cookie helpers — Phase 2.
// Stub so imports resolve from Phase 1 onward.
// Implementation (ADMIN_PASSWORD_HASH verification + SESSION_SECRET signing) added in Phase 2.

export function getSession() {
  // TODO (Phase 2): read and verify the session cookie
  return null;
}

export function isAuthenticated(): boolean {
  // TODO (Phase 2): real check
  return false;
}
